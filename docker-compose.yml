services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: newsweather
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: newsweather
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: